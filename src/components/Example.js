import React from 'react';
import { Button } from 'antd-mobile';

const Example = () => {
  return (
    <div>
      Example <Button>Start</Button>
    </div>
  );
};

Example.propTypes = {
};

export default Example;
